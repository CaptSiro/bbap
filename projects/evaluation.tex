\subsection{Testing the scenarios}\label{subsec:testing-the-scenarios}
This section outlines the results of implementing the scenarios.
The framework testing areas include CMS features, AI-Generation of content, and several development metrics.

\subsubsection{CMS Components}\label{subsubsec:cms-components}
The system is based on a role-based access control model, which enables the administrators to create users
and assign them to groups.
Users inherit privileges from the groups they are part of.
The framework defines several special groups as a part of initial configuration.
For example, the Admin group grants implicit access to the administration dashboard.
Even if a user are belongs a custom group,
that grants the privilege to Page editing,
without assigning the Admin group the user cannot log in to administration dashboard.

The system lacks proper descriptions for these special groups, and requires implicit understanding of the special groups.

\subsubsection{AI Generation}\label{subsubsec:ai-generation}
The framework provides couple of ways to generate content more efficiently by using AI\@.
The following table describes whether the feature was used and following paragraphs describe the quality of generated content.

\begin{table}[H]
    \centering
    \begin{tabular}{|p{0.4\linewidth}|c|c|c|}
        \hline
        \textbf{Feature}
        & \textbf{Corporate}
        & \textbf{Content-Driven}
        & \textbf{Personal}
        \\
        \hline
        Page Structure Generation
        & Yes
        & Yes
        & No
        \\
        \hline
        Article Generation
        & Yes
        & Yes
        & Yes
        \\
        \hline
        Static Page Generation
        & Yes
        & Yes
        & No
        \\
        \hline
        Page Component Generation
        & Yes
        & No
        & No
        \\
        \hline
        Documentation Generation
        & No
        & No
        & Yes
        \\
        \hline
    \end{tabular}
    \caption{Testing Section - AI Generation Overview}
    \label{tab:evaluation-test-ai-overview}
\end{table}

\paragraph{Page Structure Generation}
The generated structures are usable but a few pages need to be removed for each project.
For example, sometimes the Home page needs to be removed because it is implemented via the Home component.
A few times the AI generated the whole structure with Home being the parent.
Due to the system limitation not allowing transferring parent-child relation to other pages the whole structure
has to be deleted and regenerated.

In the content-driven scenario, the generated structure was ready to use and only blog articles had to be filled in manually.

\paragraph{Article Generation}
The generated output matched the desired content but the Markdown formatting had to be manually adjusted.
This is due to the use of a custom-made Markdown compiler, where some AI-Generated formatting choices
lead to undefined results.

\paragraph{Static Page Generation}
The static contact pages are generated using AI with simple prompt without styling guides
and detailed description of each scenario.
Combined with the use of a smaller model, this led to lower-quality results with mismatched theme styling
and incorrect typography.

\paragraph{Page Component Generation}
The page content generation is used to generate static table for corporate scenario.
With detailed prompt the output is visually presentable but not directly usable in production.
The issue is that generated components cannot expose editable properties or content to the editor.
If the user wants to edit the content, the change must be explicitly stated in the prompt rather than editing the output directly.

This makes the feature well-suited for drafting components in natural language and handing the output to development team
to implement fully functional component.

\paragraph{Documentation Generation}
The Ai-Generated documentation outlines source files and lists methods of defined within each file.
The introduction is concise, the same applies to the method descriptions.
Due to the probabilistic nature of LLMs, the generated output varies in correctness, which is problematic in fact-driven environment,
such as technical documentation.
Additionally, the output is in Markdown format and inherits the same problems with formatting as Article Generation.

This means that for each documentation page the project maintainer needs to verify the correctness of the generated file,
and fix formatting before it can be declared production ready.
The issue is that the framework uses on-demand documentation generation,
which may expose users to incorrect and visually broken technical documentation.

This design decision introduces a trade-off between accessibility and reliability of the documentation.

\subsubsection{Metrics Evaluation}\label{subsubsec:metrics-evaluation}
The framework is tested on the metrics defined in the table.
The evaluation focuses on development effort, responsiveness, and SEO\@.
If critical bug is encountered during the deployment, the development of fixing the bug is not counted to the metrics.
Some metrics share identical values because they are influenced by framework-level features
rather than by scenario-specific implementation details.

\begin{table}[H]
    \centering
    \begin{tabular}{|p{0.32\linewidth}|c|c|c|}
        \hline
        \textbf{Evaluation Metric}
        & \textbf{Corporate}
        & \textbf{Content-Driven}
        & \textbf{Personal}
        \\
        \hline
        Requirement Coverage
        & 100\%
        & 100\%
        & 50\%
        \\
        \hline
        Manual Interventions
        & 1
        & 1
        & 6
        \\
        \hline
        Time to Publishable Draft
        & 1 hour
        & 2 hours
        & 10+ hours
        \\
        \hline
        Public Page Responsiveness
        & Yes
        & Yes
        & Yes
        \\
        \hline
        Administration Responsiveness
        & Desktop Only
        & Desktop Only
        & Desktop Only
        \\
        \hline
        Basic SEO Readiness
        & Partial
        & Partial
        & Partial
        \\
        \hline
    \end{tabular}
    \caption{Metrics Evaluation Overview}
    \label{tab:evaluation-metrics-overview}
\end{table}

\paragraph{Requirement Coverage}
In corporate and content-driven scenarios, all scenario-specific are implemented by the framework.
The only manual implemented part is custom homepage for each scenario, which is expected in framework deployment workflow

In the personal scenario, all CMS features are implemented by the framework but extension-specific features
must be implemented manually.
This includes custom HTTP endpoints, a custom extension component with freeform widget editor different
from webpage widget component editor, static images, extension metadata, and custom homepage.

As a result, the requirement coverage for the personal scenario is estimated at approximately 50\%.
The scenario consists of two major parts: extension-specific functionality and CMS-based functionality for advertisement and documentation.
The framework directly supports only the CMS-related functionality, while the extension-specific systems must be implemented manually.

As a result, the requirement coverage for the personal scenario is estimated at approximately 50\%.
The scenario consists of two major parts: extension-specific features and CMS-based features for advertisement and documentation.
The framework offers tools tailored only to the CMS-related functionality, while extension-specific part needs to be implemented manually.

\paragraph{Manual Interventions}
All scenarios must implement custom homepage which counts as one manual intervention.
The personal scenario also includes extension-specific features:

\begin{itemize}
    \item Custom HTTP endpoints
    \item Extension component
    \item Freeform widget editor
    \item Static images
    \item Extension metadata
\end{itemize}

\paragraph{Time to Publishable Draft}
Measured from the initial commit to the final commit related to deployment.
Time spent bug fixing and updating the framework are excluded from the measurement.
For the personal scenario the initial commit already includes most of the implementation due to incorrect project setup.
This means, that time measurement metric can be estimated but not calculated.

\paragraph{Responsiveness}
All publicly accessible pages are responsive across tested device sizes.
Although, some elements are implemented functionally but not optimized for usability.
The administration dashboard interface require custom implementation of core user controls, and it is currently optimized
for desktop devices and standard landscape resolutions.

\paragraph{Basic SEO Readiness}
Publicly accessible pages satisfy the Chrome Lighthouse SEO test~\parencite{chrom_lighthouse}.
The homepage is as exception because it requires description to be added by manually editing the Home component.
