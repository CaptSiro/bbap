\chapter{System Design}\label{ch:system-design}
This section describes how the system works without going into the implementation details.
Painting the whole system as a black-box implementation, the interface is known, but the underlying implementation is not.

\section{Design Goals}\label{sec:design-goals}
The primary objective of the system is to provide a flexible environment for full-stack development.
The design emphasizes simplicity and ease of integration, allowing the components to be extended easily.

The key design goals are:
\begin{itemize}
    \item Simplicity - The system interface is simple and the implementation of programming patterns is consistent.
    \item Modularity - The system is divided into components.
    \item Extensibility - New functionality can be added to existing components.
    \item Automation - Repetitive development tasks should be automated with AI if possible.
    \item Transparency - The system should expose clear interface for each component.
\end{itemize}

These goals reflect the prototype nature of the system, which aims to explore automation of common full-stack development tasks.

\input{design/components}

\section{System Data Flow}\label{subsec:data-flow-and-integration}
All components have access to the databases that act as a shared representation of project data.
Environment settings are not limited to using database as persistent data storage.
Some values are better stored as constant values.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\linewidth]{images/c4-diagram}
    \caption{Architecture}
    \label{fig:architecture}
\end{figure}

The typical workflow consists of the following steps:

\begin{enumerate}
    \item Creation of request-specific data objects and establishing database connections.
    \item Execution of actions responsible for handling the request.
    \item Actions run the business logic using database records or delegate processing to other system components.
    \item Response is aggregated and rendered in desired format to the user.
\end{enumerate}

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        [
        node distance=2.5cm,
        rclane/.style={draw, minimum width=15cm, minimum height=1.5cm, anchor=west},
        rcactivity/.style={draw, rectangle, minimum width=2.8cm, minimum height=0.8cm},
        rcarrow/.style={->, thick}
        ]

        \node[rclane, label={[anchor=west]west:\textbf{Client}}] (client) at (0,4) {};
        \node[rclane, label={[anchor=west]west:\textbf{Routing}}] (routing) at (0,2.5) {};
        \node[rclane, label={[anchor=west]west:\textbf{Actions}}] (actions) at (0,1) {};
        \node[rclane, label={[anchor=west]west:\textbf{Components}}] (components) at (0,-0.5) {};
        \node[rclane, label={[anchor=west]west:\textbf{Database}}] (db) at (0,-2) {};

        \node[rcactivity] (req) at (3,4) {Request};
        \node[rcactivity] (response) at (13,4) {Response};

        \node[rcactivity] (route) at (5,2.5) {Action Selection};

        \node[rcactivity] (action) at (7,1) {Execute Action};

        \node[rcactivity] (logic) at (11,-0.5) {Business Logic};

        \node[rcactivity] (env) at (9,-2) {Environment};
        \node[rcactivity] (query) at (13,-2) {Records};

        \draw[rcarrow, blue] (req) -- (3,2.5) -- (route);
        \draw[rcarrow, blue] (route) -- (5,1) -- (action);
        \draw[rcarrow, blue] (action) -- (7,-0.5) -- (logic);
        \draw[rcarrow, blue] (action) -- (7,-2) -- (env);
        \draw[rcarrow, blue] (logic) -- (11,-2) -- (query);

        \draw[rcarrow, red] (route) -| node[pos=0.25, above] {Action not found} (response);
        \draw[rcarrow, red] (action) -| node[pos=0.25, above] {Bad request} (response);

        \draw[rcarrow, green] (logic) -| (response);

        % Legend
        \node[draw, align=left, anchor=north west] at (0,-3) {
            \begin{tikzpicture}[arrow/.style={->, thick}]
                \draw[arrow, blue] (0,0) -- (1,0) node[right, black] {Normal flow};
                \draw[arrow, red] (0,-0.5) -- (1,-0.5) node[right, black] {Error flow};
                \draw[arrow, green] (0,-1) -- (1,-1) node[right, black] {Successful response};
            \end{tikzpicture}
        };
    \end{tikzpicture}
    \caption{System Request Processing}
    \label{fig:systemRequestProcessing}
\end{figure}

This swimming lanes diagram shows lifetime of request with database access.
User sends request to access a resource and the system finds appropriate action or actions responsible for handling the request.
The actions may not be found for multiple reasons, for example, actions are not found,
or required properties are not present in the request.
When multiple actions are found, they are executed in predefined order.
Action execution describes the process of performing all found actions.
The process may result in a failure at any point of the execution, if it is encountered the execution terminated immediately,
and response containing failure details is sent to the user.
Actions are abstraction for any arbitrary code that generates response based on the request or forwards the responsibility to another action.
Components are concrete instances of the code.
In this example, the component accesses environment data stored in database, which may include system settings or user privileges,
and reads additional records tied to the requested resource.
If no failures are encountered, successful response is generated from all the gathered inputs and the response is sent.


\section{Summary}\label{sec:summary}
The proposed design emphasizes simplicity, abstraction, and extensibility.
Designing modern systems requires solving domain-specific problems while satisfying system constraints.
Simplicity and extensibility are two constraints that work against each other.
Simple solutions suffer from architectures that are difficult to extend, while extensible systems are overly reliant
on abstraction, leading to confusing APIs.
Implementing a complex design is an act of balancing between rigid architecture and convoluted abstractions.

The proposed design satisfies the NFR6.